using System;

namespace GameOfLife
{
    public class World
    {
        public char[,] buildMatrix()
        {
            char[,] matrix = new char[32,32];
            foreach ()
            for (int i=0; i<=matrix.Length; i++)
            {

            }
            return matrix;
        }
    }
}